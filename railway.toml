# Railway — ProconBot Jacareí
# Dois serviços: proconbot (app principal) + ollama (embeddings)

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# MongoDB — copie para .env e ajuste os valores
# URI do cluster (Railway/Atlas). O nome do banco vem em MONGODB_DB_NAME se a URI não tiver /nome_do_banco
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB_NAME=proconbot_jacarei

# ERP / API HTTP
# Porta do servidor HTTP (dashboard + KPIs). Padrão: 3000
HTTP_PORT=3000

# Segurança JWT — obrigatório para rotas protegidas do ERP
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_secreta_super_forte_aqui

# Rounds do bcrypt para hash de senhas de usuários do dashboard
BCRYPT_SALT_ROUNDS=12

# Bot -> API de agendamento
# Localmente, use a URL da API REST iniciada com npm run api:dev.
AGENDAMENTO_API_BASE_URL=http://localhost:3000

# Gemini API — obrigatório para RAG (busca semântica + geração com LLM)
# Obtenha gratuitamente em: https://aistudio.google.com/apikey
# Sem esta chave, o sistema usa busca por keyword como fallback.
GEMINI_API_KEY=sua_chave_aqui

# Primeiro admin: ADMIN_EMAIL, ADMIN_SENHA e npm run api:seed-admin

# WhatsApp - informe o numero do WhatsApp em formato internacional
# para ativar o pareamento por codigo em vez de QR.
# O valor pode ser salvo com pontuacao; o sistema normaliza automaticamente.
# Ex.: +55 (11) 99999-9999 -> 5511999999999
# WHATSAPP_PHONE_NUMBER=5511999999999

# Opcional: solicita notificacao no celular durante o pareamento por codigo.
# Em Railway/headless, false costuma ser mais estavel para gerar o codigo no log.
# WHATSAPP_PAIRING_SHOW_NOTIFICATION=false

# Opcional: apos falhas repetidas no codigo, reinicia usando QR no log.
# WHATSAPP_PAIRING_FALLBACK_QR=true
# WHATSAPP_PAIRING_MAX_ATTEMPTS=3

# Opcional: diretorio da sessao do WhatsApp.
# Em containers, aponte para um volume persistente para evitar novo login em redeploy.
# Ex.: WHATSAPP_AUTH_PATH=/data/.wwebjs_auth
# WHATSAPP_AUTH_PATH=.wwebjs_auth

# Opcional: identificador da sessao usada por LocalAuth/RemoteAuth.
# Altere para um valor novo quando precisar descartar uma sessao antiga no MongoDB
# sem apagar documentos manualmente.
# WHATSAPP_AUTH_CLIENT_ID=proconbot-jacarei

# Opcional: liga logs do Chromium/Puppeteer e do console da pagina.
# Use "true" para diagnosticar falhas no pareamento por codigo na Railway.
# WHATSAPP_BROWSER_LOGS=false

# Opcional: User-Agent usado pelo whatsapp-web.js.
# O padrao do pacote ainda simula Chrome 101, que pode falhar no pareamento por codigo.
# WHATSAPP_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/139.0.0.0 Safari/537.36
